function max(a, b) {
  return a > b ? a : b;
}

console.log(max(parseInt('7'), parseFloat('9.2')));

/**
 * parseInt: converts strings to numbers
 * parseFloat: converts strings to floating point numbers
 *
 * Common Alternative:
 * Number()
 */
